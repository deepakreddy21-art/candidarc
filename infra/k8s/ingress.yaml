apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: { name: candidarc, namespace: candidarc }
spec:
  rules:
    - host: candidarc.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: { service: { name: web, port: { number: 80 } } }
